# python:S1578 keys on the module file stem, not on content: the stem
# "s1578_bad" already satisfies the naming convention regex, so no content
# can trigger the rule under the fixed fixture naming (skip:"filename").
value = 1
