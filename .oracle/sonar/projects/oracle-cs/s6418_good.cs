class S6418Good
{
    private const string ApiKey = "placeholder";

    void Send(string token)
    {
        var label = "token";
        Use(label, token);
    }

    void Use(string label, string value) { }
}
