public class S2757Good
{
    public void Work(int amount, int step)
    {
        int total = 0;
        total = +amount;
        int delta = -step;
        if (amount == -step)
        {
            System.Console.WriteLine(delta);
        }
    }
}
