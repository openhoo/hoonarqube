class Conversions
{
    void All()
    {
        float scale = (float)1.5;
        double precise = (double)2.25;
        int whole = (int)42;
        char initial = (char)'i';
        bool flag = (bool)true;
        string label = (string)"value";
    }
}
