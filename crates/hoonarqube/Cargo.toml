[package]
name = "hoonarqube"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
hoonarqube-catalog = { path = "../hoonarqube-catalog" }
hoonarqube-core = { path = "../hoonarqube-core" }
hoonarqube-ir = { path = "../hoonarqube-ir" }

[lints]
workspace = true
