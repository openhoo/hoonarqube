[package]
name = "hoonarqube-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"
[dependencies]
hoonarqube-catalog = { version = "0.4.1", path = "../hoonarqube-catalog" }
hoonarqube-csharp = { version = "0.4.1", path = "../hoonarqube-csharp" }
hoonarqube-go = { version = "0.4.1", path = "../hoonarqube-go" }
hoonarqube-ir = { version = "0.4.1", path = "../hoonarqube-ir" }
hoonarqube-jsts = { version = "0.4.1", path = "../hoonarqube-jsts" }
hoonarqube-java = { version = "0.4.1", path = "../hoonarqube-java" }
hoonarqube-python = { version = "0.4.1", path = "../hoonarqube-python" }
hoonarqube-ruby = { version = "0.4.1", path = "../hoonarqube-ruby" }
hoonarqube-rust = { version = "0.4.1", path = "../hoonarqube-rust" }

[lints]
workspace = true
